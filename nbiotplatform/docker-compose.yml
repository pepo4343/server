version: "3.2"
services:
  udpserver:
    build: ./udpserver
    container_name: udpserver
    restart: unless-stopped
    networks:
      - nginx_default
networks:
  nginx_default:
    external: true
